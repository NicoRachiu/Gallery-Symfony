{% extends "admin.html.twig" %}

{% block content %}

<div id="page-wrapper">

    <div class="container-fluid">

        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">
                    USERS
                </h1>
                <a href="/user-add" class="btn btn-primary">ADD USER</a>
                <div class="col-md-12">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>id</th>
                                <th>Photo</th>
                                <th>Username</th>
                                <th>First Name</th>
                                <th>Last Name</th>
                            </tr>
                        </thead>
                        <tbody>
                        
                            {% for user in users %}
                                <tr>
                                    <td>{{ user.id }}</td>
                                    <td><img class="user-image" src="">

                                    </td>
                                    <td> {{ user.username }}
                                        <div class="actions_links">
                                            <a href="/user-delete?id={{ user.id }}">Delete</a> |
                                            <a href="user-edit?id={{ user.id }}">Edit</a>
                                        </div>
                                    </td>

                                    <td> {{ user.username }}</td>
                                    <td> {{ user.username }}</td>
                                </tr>
                            {% endfor %}
                            
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
</div>
<!-- /#page-wrapper -->

{% endblock %}